# 設計

フラグメントの今の実装の設計と、その理由。導入手順は [README.md](README.md)。

## 1. 目指すもの

- **AI は用件を預かる係** — 目的は相手の用件を聞き出して本人に渡すことで、解決ではない。
  相手に言いたいことを言わせ、相槌と聞き返しで情報を集める。本人は記録を見て、準備してから折り返す
- **本人はいつでも割り込める** — 応対中の会話をライブで見て、必要なら本人が出る。AI は聞き役に下がる
- **普通の電話として使える** — 着信・発信・マナーモード・おやすみモードの扱いを、端末の電話と揃える
- **回線を持たない** — システムは SIP の接続情報を預かるだけ。番号の契約者は利用者本人

## 2. 着信の流れ

```
着信 (SIP)
 │
 ├─ 1. 関門 (hookd /guard) ……… 不正利用なら、呼び出し音も鳴らさず切る
 ├─ 2. 取る → 録音告知「この通話は録音されます」(音声ファイル)
 │      同時に: 端末へ着信を知らせる (相手の表示だけ。出るボタンと着信音はまだ)
 │              下調べ (電話帳・過去の通話) と番号の web 検索を開始
 ├─ 3. 番号スクリーニング ……… 明らかな営業・詐欺ならここで切る
 ├─ 4. 呼び出し (相手には呼び出し音、端末には出るボタンと着信音)
 │      応答モードに従って待つ:
 │        不在       … 待たない
 │        スタンバイ … 数秒待つ
 │        自分で出る … 本人が出るまで待つ (AI は出ない)
 │      端末の答え: 出る → 本人につなぐ / AI に任せる → すぐ AI / 切る → 相手ごと切る
 └─ 5. LiveKit のルームへ
        AI が出る場合 … 挨拶して応対。会話は文字起こしされ、吹き出しが浮かぶ
        本人が出た場合 … AI は喋らない書記として同席 (文字起こしと吹き出しは続ける)
```

- **取ってから告知する** — 電話を取った時点で発信側に課金が始まるので、ワンギリ (1コールで切って折り返させる手口)
  はここで無意味になる。告知を流している約2秒は、下調べと番号検索の待ち時間にも使える
- **出るボタンは告知の後** — 告知が終わる前に本人がつながると、告知が本人の通話に被る
- **定型文は音声ファイル** — 録音告知はダイヤルプランが流す。TTS の待ちも、相手の声による遮りも無い

## 3. 部品

### Asterisk (回線の仲介)

LiveKit SIP はトランク型 (INVITE を直接受ける) だけで、回線に REGISTER する機能を持たない。
050 などの REGISTER 型回線のために Asterisk を仲介に置き、上の着信の流れ (ダイヤルプラン) もここに書く。
呼び出し音は保留音の仕組みでループ再生する (hookd への問い合わせで待っている間も途切れない)。

### LiveKit (1通話 = 1ルーム)

着信も発信も、1本の通話を1つのルームに対応させる。応対 AI・本人 (スマホ / 管制室)・相手が同じルームに入る。
本人の参加者は `operator-` で始まる ID で入り、agent はそれを見て聞き役に下がる。

### agent (応対 AI)

- AI コアは Gemini Live API (音声→音声)。電話は賢さより遅延が先に体験を壊すため、音声を直接扱う構成にしている
- 手元の VAD (Silero) を併用する。モデルのターン検出だけでは、相手が話し始めたことが割り込み時にしか分からない
- 相手の発話中に届いた「後方支援メモ」と本人の「耳打ち」は、会話の文脈にラベル付きで差し込む
- 取り次ぎ: 相手が本人と直接話したがったら端末を呼ぶ。本人が断るか時間切れなら「つかまりませんでした」と伝えて用件を預かる
- 本人が受話ボタンで取った通話は、応答モードの設定によらず書記モード (AI は喋らない) になる

### hookd (着信フック API)

ダイヤルプランと端末の間に立つ小さな HTTP サーバー。警備の関門、下調べの起動、着信状態 (告知中 / 呼び出し中) の管理、
受話・AI に任せる・切るの受け付け、取り次ぎ、発信、FCM で端末を起こす。状態はメモリに持つ。

### directory-agent (ワークスペースの番人)

- **下調べ**: 電話帳 (`連絡先/<番号>.md`) と過去の通話をまとめる
- **番号検索**: 発信者番号を Google 検索付きの Gemini で調べ、事業者名と「通常 / 営業 / 詐欺 / 不明」を返す。
  営業・詐欺は明確な報告がある場合だけ (誤判定は正当な着信の自動拒否につながる)
- **会話の見張り**: 相手の新しい発話があるたびに、本人向けの吹き出しと、応対 AI 向けの後方支援メモを分けて出す。
  吹き出しは本人しか見ないので、本人限定の情報も書ける。応対 AI 向けのメモは開示の規則に従う
- **終話の要約**: AI が受けた通話が終わったら、通知用の短い要約を作る
- **通話記録**: 終わった通話をワークスペースに Markdown で書き出す

### 管制室 (Next.js)

Web の UI。着信バナー (3択)、ライブの通話画面 (文字起こし・吹き出し・耳打ち・交代)、履歴、設定、端末のペアリング (QR)。
Google OAuth でログインし、許可したアカウントだけが入れる。スマホアプリは同じ API を端末トークンで叩く。

### Android アプリ

- 自己管理型の通話 (Telecom の self-managed ConnectionService) として着信を登録する
- **着信画面と着信音は通知が出す** — ロック中は全画面、使用中は上部のバナー (OS が出し分ける)。
  着信音は通知チャンネルの音なので、マナー / バイブ / おやすみモードが普通の電話と同じ判定になる
  (アプリ自身が鳴らす音は、おやすみモードで OS に消される)
- 状態はサーバーへのロングポーリングで受け、FCM で起こす
- 取り次ぎでは、判断材料 (吹き出しと直近の会話) を鳴っている間も更新する。ロック中は本人限定の情報を描かない

## 4. データ

PostgreSQL (`infra/postgres/init.sql`):

| テーブル | 中身 |
|---|---|
| `calls` | 通話1本。向き、発信者、応対者 (`ai` / `human` / `ai_then_human`)、開始・終了、要約 |
| `transcript_segments` | 文字起こし (話者: 相手 / AI / 本人 / 耳打ち) |
| `fragments` | 吹き出し |
| `agent_jobs` | 下調べ・番号検索・後方支援メモ・耳打ちのキュー |
| `settings` | 応答モード・声の設定など |
| `security_events` / `call_attempts` / `blocked_callers` | 警備の記録・レート制限の母数・ブロック |
| `device_push_tokens` | 端末のプッシュ宛先 |

ワークスペース (Markdown): `連絡先/` (電話帳)、`通話記録/`、`共有メモ/`、`受信箱/`、`設定/` (応対 AI の人格・情報の扱い)。
人が読み書きする情報は DB ではなくここに置き、本人や他のツールが直接編集できるようにする。

## 5. 設計上の判断

- **fail-open** — 警備・下調べ・番号検索が落ちていても、着信は通す。警備が着信を止める方が損害が大きい
- **定型文は確定再生** — 挨拶や告知は、相手の声で遮られると「言えなかった」で済まない。音声ファイルか割り込み無効の再生で流す
- **応答モードは回線全体、一時停止は端末ごと** — 「誰が鳴るか」は端末ごとに止められるが、AI が出るかどうかは回線で1つ
- **本人が取ったかは受話ボタンが知っている** — 応答モードの設定から推測しない
- **通知は終話後の1枚** — 応対中は端末に何も出さない (AI 応対中の表示は設定で選べる)
- **開発用の道具は製品に置かない** — テスト通話やシナリオ試験は `infra/scripts/` の外部ツール
